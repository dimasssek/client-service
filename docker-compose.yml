# DEPRECATED: use bsps-infra/docker-compose.yml instead.
# This file is kept for reference only.

services:
  postgres:
    image: postgres:16
    container_name: client-service-postgres
    ports:
      - "5488:5432"
    environment:
      POSTGRES_DB: client-service
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - client-service-postgres-data:/var/lib/postgresql/data

  rabbitmq:
    image: rabbitmq:3-management
    container_name: client-service-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest

volumes:
  client-service-postgres-data:
