spring:
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true
  rabbitmq:
    listener:
      simple:
        auto-startup: false
  datasource:
    hikari:
      maximum-pool-size: 5
      connection-timeout: 10000

app:
  emulator:
    enabled: false
